% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{conditional_permutation}
\alias{conditional_permutation}
\title{Permute Y conditioned on B or G}
\usage{
conditional_permutation(
  Y,
  B = NULL,
  G = NULL,
  num = 1000,
  seed = NULL,
  duplicates.ok = TRUE
)
}
\arguments{
\item{Y}{A numeric vector of length n. The variable to permute.}

\item{B}{A factor (or object coercible via \code{split()}) of length n
indicating batch membership. Permutations are performed within each level of `B`.}

\item{G}{A factor (or object coercible via \code{split()}) of length n
indicating group membership. Permutations are performed within each level of `G`
if `B` is not specified.}

\item{num}{Positive integer. Number of permutations to generate. \cr
Default: 1000}

\item{seed}{An integer random seed for reproducibility. \cr
Default: NULL}

\item{duplicates.ok}{Logical. If FALSE, duplicate permutation rows are
removed from the output, which may result in fewer than `num` columns.\cr
Default: TRUE}
}
\value{
A numeric matrix of dimensions n × m, where `m <= num`. 
Each column is one permutation of `Y` within batch levels.
}
\description{
Permute Y conditioned on B or G
}
\keyword{internal}
